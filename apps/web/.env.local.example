# 공개 웹 앱 환경변수
# 이 파일을 복사해서 .env.local을 만들고 실제 값으로 채우세요.
# cp .env.local.example .env.local

# Neon PostgreSQL 연결 문자열 (읽기 전용 권한 권장)
DATABASE_URL=postgresql://[readonly_user]:[password]@[host]/[database]?sslmode=require
